<UserControl x:Class="FlightTracker.Views.UserControls.AircraftMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svg1="clr-namespace:SVGImage.SVG;assembly=DotNetProjects.SVGImage"
             xmlns:local="clr-namespace:FlightTracker.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="40">
    <svg1:SVGImage x:Name="Icon" Source="/FlightTracker;component/Resources/marker.svg"
        Width="40"
        Height="40"
        VerticalContentAlignment="Center"
        HorizontalContentAlignment="Center" RenderTransformOrigin="0.5,0.5" >
        <svg1:SVGImage.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform/>
                <RotateTransform x:Name="IconRotTransform"  Angle="0"/>
                <TranslateTransform/>
            </TransformGroup>
        </svg1:SVGImage.RenderTransform>
    </svg1:SVGImage>
</UserControl>
